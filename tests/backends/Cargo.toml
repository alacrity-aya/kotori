[package]
name = "test-backend"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tokio = { version = "1.48.0", features = ["full"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
